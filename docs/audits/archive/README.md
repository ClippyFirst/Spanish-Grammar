# Archived audits

This directory contains historical audit snapshots retained for provenance.

These files describe the repository at the time they were written. They are not current source-of-truth documents and should not be edited to make them look current.

For the active audit state, use `docs/audits/current/`. For repository architecture and behavior, use `README.md`, `package.json`, and the source/configuration under `src/`.
