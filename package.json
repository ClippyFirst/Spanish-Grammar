{
  "name": "gramaticarrona",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gramaticarrona — Spanish grammar reference for Ukrainian speakers.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "search:index": "pagefind --site dist",
    "build:full": "astro build && pagefind --site dist",
    "validate": "node scripts/validate-content.mjs",
    "links": "node scripts/check-links.mjs",
    "qa": "node scripts/validate-content.mjs && node scripts/check-links.mjs && astro build",
    "check": "astro check",
    "audit:graph": "node scripts/audit-content-graph.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.0.0",
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.2.0",
    "pagefind": "^1.2.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "allowScripts": {
    "esbuild@0.27.7": true,
    "esbuild@0.25.12": true,
    "sharp@0.34.5": true
  }
}
