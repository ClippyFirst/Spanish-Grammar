import fs from 'node:fs';import path from 'node:path';const R=process.cwd(),D=path.join(R,'src');const files=[];function w(x){for(const e of fs.readdirSync(x,{withFileTypes:true})){const p=path.join(x,e.name);e.isDirectory()?w(p):files.push(p)}}w(D);const css=files.filter(x=>x.endsWith('.css')).map(x=>fs.readFileSync(x,'utf8')).join('\n');const min=[...css.matchAll(/@media\\s*\\([^)]*min-width\\s*:\\s*(\\d+)px/g)].map(m=>+m[1]),max=[...css.matchAll(/@media\\s*\\([^)]*max-width\\s*:\\s*(\\d+)px/g)].map(m=>+m[1]);console.log('min breakpoints:',[...new Set(min)].sort((a,b)=>a-b).join(', ')||'none');console.log('max breakpoints:',[...new Set(max)].sort((a,b)=>a-b).join(', ')||'none');console.log('overflow guards:',/overflow-x\\s*:\\s*auto/.test(css));console.log('mobile navigation:',/mobile-menu/.test(css));console.log('mobile TOC:',/toc-mobile/.test(css));if(!min.length||!max.length)process.exitCode=1;