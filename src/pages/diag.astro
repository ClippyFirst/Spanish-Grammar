---
// Diagnostic: validates ONLY scratch without a schema to see if frontmatter parses.
import { getCollection } from 'astro:content';

export async function getStaticPaths() {
  const all = await getCollection('es');
  const e = all.find((x) => x.id.endsWith('scratch'));
  return [];
}
---
<!doctype html><html><body>diag</body></html>